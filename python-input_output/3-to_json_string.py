#!/usr/bin/python3
"""_summary_ """
import json


def to_json_string(my_obj):
    """ The above code is converting the Python
            object my_obj into a JSON string."""
    return json.dumps(my_obj)
